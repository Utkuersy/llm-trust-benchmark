# --- Core ---
pydantic>=2.6,<3.0
PyYAML>=6.0,<7.0
numpy>=1.26,<3.0
pandas>=2.1,<3.0

# --- Evaluation ---
sympy>=1.12,<2.0          # math answer equivalence (symbolic comparison)

# --- MLOps ---
mlflow>=2.12,<3.0

# --- Dashboard ---
streamlit>=1.33,<2.0
plotly>=5.20,<6.0

# --- Development ---
pytest>=8.0,<9.0
ruff>=0.4,<1.0
mypy>=1.9,<2.0
pylint>=3.1,<4.0
bandit[toml]>=1.7,<2.0
pip-audit>=2.7,<3.0
pre-commit>=3.7,<5.0
types-PyYAML>=6.0,<7.0

# --- Optional: real embedding, vector DB, and LLM-based evaluation ---
# If not installed, the system falls back in order to a NumPy store, a
# hashing embedding, and a heuristic faithfulness mode; the backend used is
# reported in the output's "backend" field.
#
# INTERNAL-NETWORK NOTE: these packages try to download model weights at
# runtime. In an air-gapped environment, download the weights ahead of time,
# put them on disk, and run with HF_HUB_OFFLINE=1 / TRANSFORMERS_OFFLINE=1
# (config/offline).
#
# chromadb>=0.5,<0.7
# sentence-transformers>=2.7,<4.0
# transformers>=4.40,<5.0      # the content_safety classifier layer
# ragas>=0.1.9,<0.3
# datasets>=2.18,<4.0
